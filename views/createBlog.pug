extends layout

block content
  h2 Add Blog
  div.flex.two
    form(method='POST', action='/blogs/add')
        label Title:
        input(name='title', type='text')
        label Body:
        textarea(name='body')
        input(type='submit', value='Submit')
